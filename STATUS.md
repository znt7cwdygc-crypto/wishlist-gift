# ✅ Сервер запущен!

## 🌐 Доступные URL:

- **Главная**: http://localhost:3000
- **Модель**: http://localhost:3000/model  
- **Даритель**: http://localhost:3000/donor
- **Админ**: http://localhost:3000/admin

## 📊 Статус:

Сервер запущен в фоновом режиме.

## 🔍 Проверка работы:

Откройте браузер и перейдите на http://localhost:3000

Или проверьте через терминал:
```bash
curl http://localhost:3000
```

## 🛑 Остановка сервера:

Если нужно остановить сервер, найдите процесс:
```bash
ps aux | grep "node server/index.js"
kill <PID>
```

Или используйте:
```bash
pkill -f "node server/index.js"
```


